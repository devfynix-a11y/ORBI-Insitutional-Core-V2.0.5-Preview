
/**
 * CORE IDENTITY RESOLVER
 * Centrally managed identity node for DilPesa cluster.
 */
import { getSupabase, getAdminSupabase } from '../services/supabaseClient.js';

export { getSupabase, getAdminSupabase };
