# TLS Certificates

For local testing, place `fullchain.pem` and `privkey.pem` here. These files are
ignored by Git.

Production should set `ORBI_TLS_CERT_DIRECTORY` to a root-owned directory
outside the repository.
